import { MediaCompeticaoCanto } from './media-competicao-canto';

describe('MediaCompeticaoCanto', () => {
  it('should create an instance', () => {
    expect(new MediaCompeticaoCanto()).toBeTruthy();
  });
});
